#Ignore the UFT object repos because they are binary and cause merge conflicts
*.tsr

#Lock files that UFT generates when a file is open
*.lck

#Temp excel file when an excel sheet is open
~*.xls*

# Report folder
**/Report

# Pdfs that are generated by NGQ
*.pdf